[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cleancms"
version = "0.1.0"
description = "DuckDB-based post-normalization cleaning pipeline for CMS data"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "duckdb",
    "pyyaml",
    "hydra-core",
    "omegaconf",
]

[tool.setuptools]
packages = ["cleancms"]

[project.scripts]
create_dir_paths = "cleancms.create_dir_paths:main"